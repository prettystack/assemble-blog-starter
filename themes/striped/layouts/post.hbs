---
layout: default.hbs
---


<article class="is-post">

  <header>
    <h2><a href="#">{{ title }}</a></h2>
    <span class="byline"> {{ subtitle }} </span>
  </header>

  <div class="info">
    <span class="date">
      <span class="month">{{ formatDate date "%b"}}<span></span></span> <span class="day">{{ formatDate date "%d"}}</span><span class="year">, {{ formatDate date "%Y"}}</span>
    </span>

    <ul class="stats">
      {{#if config.disqus.shortname }}
        <li><a href="{{ url }}#disqus_thread" class="fa fa-comment">?</a></li>
      {{/if}}
    </ul>
  </div>

  {{#if image }}
    <a href="{{ url }}" class="image image-full"><img src="/images/{{ image }}" alt="" /></a>
  {{/if}}

  <div class="post">
    {{#markdown}}
      {{> body }}
    {{/markdown}}

    {{> disqus_thread }}
  </div>

</article>
